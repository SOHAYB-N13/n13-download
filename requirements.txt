# Runtime dependencies
requests>=2.31
urllib3>=2.0
rich>=13.0
colorama>=0.4
pywebview>=5.0
pyfiglet>=0.8

# Optional: system stats on the dashboard (CPU/RAM/disk).
# The app works without it (those readouts show em-dashes).
psutil>=5.9

# Optional: import cookies from a live browser profile.
# The core engine still works without it.
browser-cookie3>=0.19

# Optional: one-click Chrome extension installer (Windows UI automation).
# The app works without it (the "Install Extension" button reports an error).
uiautomation>=2.0.19
pywinauto>=0.6.8
